import {describe, expect, jest, test} from '@jest/globals';
import {callFuncArray} from "../task_08.js";

describe('Вызов массива функций', () => {
    test('сортировка по возрасту', async () => {

    });
});
